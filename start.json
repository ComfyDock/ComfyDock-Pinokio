{
  "daemon": true,
  "run": [
    {
      "method": "shell.run",
      "params": {
        "message": "uv run start_server.py --allow_running_multiple_containers {{env.ALLOW_RUNNING_MULTIPLE_CONTAINERS}}"
      }
    }
  ]
}
